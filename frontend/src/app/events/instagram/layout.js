export const metadata = {
  title: "Instagram event cards",
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function InstagramEventsLayout({ children }) {
  return children;
}
